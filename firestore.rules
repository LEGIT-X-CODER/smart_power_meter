rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules — open access
    // ⚠️ Change to proper auth-based rules before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
